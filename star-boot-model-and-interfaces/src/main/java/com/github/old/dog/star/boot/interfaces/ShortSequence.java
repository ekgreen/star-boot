package com.github.old.dog.star.boot.interfaces;

/**
 * A non-sealed interface representing a sequence of short values.
 * <p>
 * This interface extends {@link Sequence} with Short as the specific type,
 * allowing implementations to provide sequences of short values.
 */
public non-sealed interface ShortSequence extends Sequence<Short> {
}
