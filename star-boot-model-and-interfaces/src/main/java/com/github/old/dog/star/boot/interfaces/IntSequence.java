package com.github.old.dog.star.boot.interfaces;

/**
 * A non-sealed interface representing a sequence of integer values.
 * <p>
 * This interface extends {@link Sequence} with Integer as the specific type,
 * allowing implementations to provide sequences of integer values.
 */
public non-sealed interface IntSequence extends Sequence<Integer> {
}
