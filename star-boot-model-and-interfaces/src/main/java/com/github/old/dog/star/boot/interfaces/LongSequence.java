package com.github.old.dog.star.boot.interfaces;

/**
 * A non-sealed interface representing a sequence of long values.
 * <p>
 * This interface extends {@link Sequence} with Long as the specific type,
 * allowing implementations to provide sequences of long values.
 */
public non-sealed interface LongSequence extends Sequence<Long> {
}
